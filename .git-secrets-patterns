# Custom git-secrets patterns for Strata-vite
# These patterns are used to prevent committing sensitive credentials

# Google API Keys (starts with AIza followed by 35 alphanumeric/dash/underscore chars)
AIza[0-9A-Za-z\-_]{35}

# Google OAuth Client IDs (numeric project ID followed by .apps.googleusercontent.com)
[0-9]+-[0-9a-z]+\.apps\.googleusercontent\.com
